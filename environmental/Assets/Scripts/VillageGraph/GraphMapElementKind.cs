namespace Environmental.VillageGraph
{
    /// <summary>
    /// Discriminator for map elements. Extend alongside new graph features.
    /// </summary>
    public enum GraphMapElementKind
    {
        Node = 0,
        Edge = 1
    }
}
